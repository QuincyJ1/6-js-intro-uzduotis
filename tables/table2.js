import multiplier from '../numbers/multiplier.js';

import { n1, n2, n3, n4, n5, n6, n7, n8, n9, n10 } from '../numbers/numbers.js';


const table2 = `${multiplier} * ${n1} = ${multiplier * skaicius}
${multiplier} * ${n2} = ${multiplier * n2}
${multiplier} * ${n3} = ${multiplier * n3}
${multiplier} * ${n4} = ${multiplier *n4  }
${multiplier} * ${n5} = ${multiplier * }
${multiplier} * ${n6} = ${multiplier * }
${multiplier} * ${n7} = ${multiplier * }
${multiplier} * ${n8} = ${multiplier * }
${multiplier} * ${n9} = ${multiplier * }
${multiplier} * ${n10} = ${multiplier * }
`


console.log(table2);

export default table2;