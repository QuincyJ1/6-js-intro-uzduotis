import multiplier from '../numbers/multiplier.js';

import { n1, n2, n3, n4, n5, n6, n7, n8, n9, n10 } from '../numbers/numbers.js';


const table2 = `${multiplier} * ${n1} = ${multiplier * n1}
${multiplier} * ${n2} = ${multiplier * n2}
${multiplier} * ${n3} = ${multiplier * n3}
${multiplier} * ${n4} = ${multiplier * n4}
${multiplier} * ${n5} = ${multiplier * n5}
${multiplier} * ${n6} = ${multiplier * n6}
${multiplier} * ${n7} = ${multiplier * n7}
${multiplier} * ${n8} = ${multiplier * n8}
${multiplier} * ${n9} = ${multiplier * n9}
${multiplier} * ${n10} = ${multiplier * n10}
`


console.log(table2);

export default table2;